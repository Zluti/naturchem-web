# Integrační release — 27. 8. 2026

## Rozsah a zdroj

- Autoritativní repozitář: `https://github.com/Ikaros277/naturchem-web.git`.
- Výchozí větev repozitáře: `main`.
- Lokální release větev: `codex/release-growth-2026-08-27`.
- Produkční release: `9d1f3bc` na `main`; obsahuje celý release commit `e74b356` a navíc EXP-011 a DATA-004.
- Výchozí produkční a rollback commit: `599f7a8` (poslední ověřený produkční stav před tímto releasem).
- Release je připraven jako jeden celek, aby vznikl nejvýše jeden produkční Vercel deployment.

## Zahrnuté změny a očekávaný obchodní dopad

| Oblast | Integrační commit | Výchozí důkaz | Změna a hypotéza |
| --- | --- | --- | --- |
| EXP-008 — článek o provozním řádu | `e624588` | Článek měl za 28 dní 183 zobrazení a 12 kliknutí v GSC, 4 organické relace a 0 `generate_lead` v GA4; ve funkcích Googlu s generativní AI měl 101 zobrazení. | Jediný kontextový box po první praktické odpovědi vede na formulář s předvolenou službou `Provozní řády`. Cílem je více kvalifikovaných přechodů do poptávky bez změny title, H1 nebo odborného výkladu. |
| EXP-009 — zachování produktu v poptávce | `d52ffcf` | Detail FID měl za 28 dní 67 zobrazení, 2 kliknutí a průměrnou pozici 30,5. Dosavadní CTA nepřenášelo konkrétní produkt. | České, anglické i německé CTA předají kategorii přístroje a lokalizovaný název produktu do formuláře. Cílem je snížit tření a zachovat obchodní kontext. |
| TECH-003 — kontrast článků na homepage | `ac9a41e` | PageSpeed 27. 8. 2026 označil `.home-editorial-cta` jako kontrastní chybu; přístupnost byla 97/100 na mobilu i desktopu. | Textová CTA používají tmavé varianty stávajících barev s kontrastem 7,24 : 1 až 8,79 : 1. Cílem je lepší čitelnost bez změny obsahu nebo rozvržení homepage. |
| EXP-010 — FID snippet | `9082fbc` | Dotaz `fid detektor` měl 25 zobrazení, 0 kliknutí a průměrnou pozici 19,0; `detektor fid` 33 zobrazení, 0 kliknutí a pozici 48,2. | Přesnější český popis kategorie a detailu vysvětluje použití FID pro GC a monitorovací přístroje. Cílem jsou relevantnější úryvky a více kvalifikovaných návštěv, nikoli samotná zobrazení. |
| TECH-004 — nulové časové ISR mezi deploymenty | tento release | Aktuální klouzavé okno má 177 523 / 200 000 ISR Write Units (88,8 %); posledních 12 hodin přidalo 1,4 tis. jednotek a 84 časových revalidací. | Homepage, Poradna, články a sitemap zůstanou plně statické. CMS Publish už spouští deployment, takže dnešní článek se aktualizuje během buildu bez průběžných ISR zápisů. Cílem je odstranit bezprostřední riziko překročení Hobby limitu. |
| Výchozí růstová data | `9e42407` | GSC a GA4 kontrolní snímek k 26. 8. 2026. | Uchovává srovnatelný baseline; nejde o tvrzení, že připravené změny už zlepšily SEO nebo konverze. |
| EXP-011 — EIA článek do přesné poptávky | `b339a2b`, produkce `9d1f3bc` | Článek měl za 28 dní 167 zobrazení, 1 kliknutí, CTR 0,6 % a pozici 5,5; v GA4 0 měřených vstupních relací a ve funkcích Googlu s generativní AI 61 zobrazení. | Jediný stručný box za první praktickou odpovědí vede na formulář s předvolenou službou `EIA a oznámení záměru`. Title, H1 a odborný výklad zůstávají beze změny. |
| DATA-004 — viditelný kontext předvyplnění | `b339a2b`, produkce `9d1f3bc` | Méně častá předvolená služba byla odeslatelná, ale návštěvník ji v krátkém seznamu neviděl. | Předvolená služba se nyní zobrazí jako první, zaškrtnutá a lokalizovaná volba. Oprava platí také pro provozní řády, IPPC, ISPOP nebo školení. |

## Ověření release kandidáta

- ESLint: 0 chyb; 17 již existujících upozornění mimo rozsah release.
- TypeScript: bez chyb, kontrola běží deterministicky s vypnutým incremental zápisem.
- Přímý test předvyplnění kontaktu: prošel pro `Provozní řády`, EIA, produktovou kategorii, lokalizovanou zprávu i omezení délky na 1 000 znaků; EIA volba je v CS/EN/DE zobrazena právě jednou se správným popiskem.
- Cache-budget test: žádná route v `src/app` nesmí používat číselný interval `revalidate`; klíčové publikační route musí mít `revalidate = false`.
- Produkční Webpack build: úspěšný; vygenerováno 558 statických stránek.
- Build přehled neobsahuje u homepage, Poradny, článků ani sitemap žádné sloupce `Revalidate` nebo `Expire`.
- URL smoke test navazujícího kandidáta: 499 URL ze sitemap a 212 dalších interních odkazů, 0 selhání.
- Routing/SEO/API smoke test: 0 selhání; ověřeny statusy, redirecty, canonical, `hreflang`, jazyk HTML, assety, 404 `noindex`, kotva formuláře a validační odpovědi kontaktního API bez odeslání poptávky.
- Vizuální kontrola homepage: desktop 1 440 × 900 px a mobil 390 × 844 px, bez horizontálního přetečení; klíčové nadpisy a CTA jsou čitelné a spodní lazy-load sekce se po posunu správně načtou.
- Článkové CTA: mobilně čitelné, vede na `/kontakt?service=Provozní%20řády#poptavkovy-formular`; odstraněna osamocená tečka za odkazem.
- Produkt FID: český meta description, H1 a CTA odpovídají změně; CS, EN a DE CTA vedou na správnou jazykovou kontaktní stránku a zachovávají název produktu.
- Článek z 27. 8. 2026 je po statickém buildu současně na homepage, v přehledu Poradny a v sitemap.
- EXP-011: desktopový i mobilní box je čitelný; mobilní formulář po přechodu zobrazuje `EIA a oznámení záměru` jako první a zaškrtnutou volbu. Formulář nebyl testovacím způsobem odeslán.

### Poznámka k přesnému `npm run verify`

Příkaz byl spuštěn. Lint, TypeScript a oba přímé testy prošly. Výchozí Turbopack build se v izolovaném worktree zastavil pouze proto, že lokální `node_modules` je sdílený junction vedoucí mimo kořen worktree. Následný produkční build stejného kódu přes Webpack prošel. V GitHub/Vercel prostředí se závislostmi nainstalovanými přímo v projektu je nutné před produkčním nasazením potvrdit standardní build.

## Měření po případném nasazení

- Primární výsledek: skutečný `generate_lead`, posuzovaný společně s kvalitou přijaté poptávky.
- EXP-008: článek → `click_inquiry_cta` → `form_start` → `generate_lead`, včetně `service_interest`.
- EXP-009: produkt → `click_inquiry_cta` → `form_start` → `generate_lead`, kategorie `pristroj` a zachovaný název produktu.
- EXP-010: po 28 dnech GSC pro oba přesné FID dotazy a obě cílové stránky; při nízkém objemu zopakovat po 56 dnech.
- EXP-011: článek → `click_inquiry_cta` → `form_start` → `generate_lead`, včetně `service_interest = EIA a oznámení záměru`; GSC a AI zobrazení jsou pouze kontrolní metriky.
- DATA-004: u první skutečné poptávky z méně časté předvolby ověřit zachování `service_interest`; nevytvářet falešný lead.
- TECH-003: po nasazení zopakovat PageSpeed na mobilu i desktopu a potvrdit odstranění kontrastní chyby.
- TECH-004: po 12 a 24 hodinách ověřit 0 časových revalidací a žádné nové ISR Write Units vyvolané běžnou návštěvou.
- Bez dostatečných dat nebude změna označena za SEO nebo konverzní zlepšení.

## Vercel a bezpečnost tarifu

Přihlášený tým `ikaros277's projects`, projekt `web-naturchem`, byl zkontrolován 27. 8. 2026. Vercel zobrazuje klouzavé 30denní období, nikoli kalendářní měsíc.

- ISR Writes při poslední kontrole: **89,0045 % z 200 000**, tedy přibližně **178 009 jednotek** a rezerva přibližně 21 991 jednotek.
- Fluid Active CPU: přibližně **3 h 13 min / 4 h** (asi 80 %).
- Image Transformations: přibližně 3 tis. / 5 tis. (60 %).
- Fast Origin Transfer: 4,84 GB / 10 GB (48 %).
- ISR Reads: 334 tis. / 1 mil. (33 %).
- Edge Requests: 275 tis. / 1 mil. (28 %).
- Function Invocations: 165 tis. / 1 mil. (16 %).

Graf ISR zápisů ukazuje velkou historickou špičku od 11. do 18. srpna. Tyto jednotky začnou z 30denního okna vypadávat přibližně od 10. září; k žádnému jednorázovému resetu na konci srpna nedojde. Při posledním 12hodinovém tempu 1,4 tis. jednotek by bez zásahu mohla současná rezerva orientačně vystačit jen asi 8 dní. Jde o extrapolaci, nikoli garanci, proto release přidává TECH-004 a počítá s jediným produkčním deploymentem.

## Produkční postup a rollback

1. Ověřit aktuální Vercel usage a správný projekt/doménu — splněno 27. 8. 2026.
2. Zkontrolovat finální diff a čistý stav release větve.
3. Vyžádat výslovný pokyn pro push navazujícího kandidáta do `Ikaros277/naturchem-web`, sloučení do `main` a jeden produkční deployment.
4. Po úspěšném standardním buildu Vercelu zopakovat smoke test proti `https://www.naturchem.cz` a neodesílat falešnou poptávku.
5. Při regresi revertovat integrační commity a jedním rollback deploymentem obnovit commit `599f7a8`.

## Stav

Release byl 27. 8. 2026 pushnut do autoritativního repozitáře `Ikaros277/naturchem-web`, sloučen do `main` a nasazen jako produkční commit `9d1f3bc`. Dne 29. 8. 2026 byla pouze čtením ověřena produkční EIA stránka: HTTP 200 a odkaz na kontaktní formulář s předvolenou službou EIA. Formulář nebyl testovacím způsobem odeslán. Následná kontrola Vercel Observability pro TECH-004 zůstává otevřená.
